import {API_KEY, AUTH_DOMAIN, PROJECT_ID, STORAGE_BUCKET, MESSAGING_SENDER_ID, APP_ID} from '@env';

export const appKey = API_KEY as string;
export const appAuthDomain = AUTH_DOMAIN as string;
export const projectId = PROJECT_ID as string;
export const storageBucket = STORAGE_BUCKET as string;
export const messagingSenderId = MESSAGING_SENDER_ID as string;
export const appId = APP_ID as string;
